{
  "name": "simple-node-app",
  "version": "1.0.0",
  "description": "Demo app for Jenkins CI/CD",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=reports/junit-results.xml"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3",
    "mocha-junit-reporter": "^2.0.0"
  }
}